<p>
  <% if User.current.admin? || User.current.allowed_to?(:view_repository_deployment_credentials, @project) %>
    <%= render template: 'repository_deployment_credentials/show' %>
  <% end %>
</p>

<p>
  <% if User.current.admin? || User.current.allowed_to?(:create_repository_deployment_credentials, @project) %>
    <%= render template: "repository_deployment_credentials/new" %>
  <% end %>
</p>

