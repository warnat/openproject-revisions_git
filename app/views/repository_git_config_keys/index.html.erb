<p>
  <% if User.current.admin? || User.current.allowed_to?(:view_repository_git_config_keys, @project) %>
    <fieldset class="repository_git_config_keys_view">
      <legend><%= "Current Git config keys" %></legend>
      <%= render template: 'repository_git_config_keys/show' %>
    </fieldset>
  <% end %>
</p>

<p>
  <% if User.current.admin? || User.current.allowed_to?(:create_repository_git_config_keys, @project) %>
    <%= render template: "repository_git_config_keys/new" %>
  <% end %>
</p>
