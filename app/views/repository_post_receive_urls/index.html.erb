<p>
  <% if User.current.admin? || User.current.allowed_to?(:view_repository_post_receive_urls, @project) %>
    <fieldset class="repository_post_receive_urls_view">
      <legend><%= "Current post receive URLs" %></legend>
      <%= render template: 'repository_post_receive_urls/show' %>
    </fieldset>
  <% end %>
</p>

<p>
  <% if User.current.admin? || User.current.allowed_to?(:create_repository_post_receive_urls, @project) %>
    <%= render template: "repository_post_receive_urls/new" %>
  <% end %>
</p>
