<% html_title 'Manage Gitolite repository' %>

<%= toolbar title: 'Manage Gitolite repository' %>




<p>
  <% if User.current.admin? || User.current.allowed_to?(:view_repository_deployment_credentials, @project) %>
    <fieldset class="fs_deployment_credentials">
      <legend><%= 'Deployment credentials' %></legend>

      <%= render template: "repository_deployment_credentials/index" %>
    </fieldset>
  <% end %>
</p>

<p>
  <% if User.current.admin? || User.current.allowed_to?(:view_repository_post_receive_urls, @project) %>
    <fieldset class="fs_post_receive_urls">
      <legend><%= 'Post receive URLs' %></legend>

      <%= render template: "repository_post_receive_urls/index" %>
    </fieldset>
  <% end %>
</p>

<p>
  <% if User.current.admin? || User.current.allowed_to?(:view_repository_mirrors, @project) %>
    <fieldset class="fs_repository_mirrors">
      <legend><%= 'Repository mirrors' %></legend>

      <%= render template: "repository_mirrors/index" %>
    </fieldset>
  <% end %>
</p>

<p>
  <% if User.current.admin? || User.current.allowed_to?(:view_repository_git_config_keys, @project) %>
    <fieldset class="fs_git_config_keys">
      <legend><%= 'Git config keys' %></legend>

      <%= render template: "repository_git_config_keys/index" %>
    </fieldset>
  <% end %>
</p>
